name: Test Runner Local Execution

on:
  push:
    branches: [ master ]
  pull_request:
    branches: [ master ]

jobs:
  test_connect:
    runs-on: self-hosted
    timeout-minutes: 2

    steps:
      - name: Show runner info
        shell: powershell
        run: |
          Write-Host "Runner hostname:"
          hostname
          Write-Host "User:"
          whoami
          Write-Host "IP:"
          ipconfig

      - name: Test local PowerShell execution
        shell: powershell
        run: |
          Write-Host "✅ Running locally on self-hosted runner"
